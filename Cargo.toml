[workspace]
resolver = "2"
members = ["crates/drift-core", "apps/drift"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/yusufani/drift"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"
